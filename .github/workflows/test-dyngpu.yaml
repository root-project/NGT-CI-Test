name: self-hosted-workflow
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: test-dyngpu-root 
    container: docker.io/rochaporto/gpu_burn
    steps:
      - name: nvidia-smi
        run: |
          nvidia-smi
